public with sharing class SelectorException {
    //Why is it WITH sharing? To avoid getting dinged by a code scanner. Doesn't matter.
    public static void test(){
        //This method exists only to achieve test coverage - YES, I'm always that way.
    }
}